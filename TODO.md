# TODO

## Contact page cleanup
- [x] Remove the GitHub redirect/template card from the contact page so only the ContactForm is displayed.
- [ ] Delete/keep the unused `components/contact/github-redirect-card.tsx` (optional) after ensuring nothing imports it.

- [ ] Run `npm run lint` and `npm run build` to verify everything compiles.


